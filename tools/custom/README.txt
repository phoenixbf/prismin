You can place here custom PRISMIN tools as sub-folders, with own package.json and all required resources

A sample tool structure would be:
    /mytool/
        - package.json
        - main.js (main entrypoint)